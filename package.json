{
    "name": "wastewise-30-root",
    "version": "1.0.0",
    "scripts": {
        "predeploy": "cd frontend && npm install && npm run build"
    },
    "dependencies": {
        "node-fetch": "^3.3.2"
    }
}
